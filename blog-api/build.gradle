dependencies {
	compile project(':blog-domain')
	compile("org.springframework.boot:spring-boot-starter-webflux:${springBootVersion}")
	
	runtime project(':blog-api')
//	runtime project(':persistence')
}
